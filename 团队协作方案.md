# 奎木狼AI小说创作系统V6 团队协作优化方案

> 创建日期：2026-04-10
> 负责人：AI架构师Agent

---

## 一、优化目标

基于架构审阅报告，制定以下优化优先级：

| 阶段 | 任务 | 负责人 | 优先级 |
|------|------|--------|--------|
| 阶段一 | 数据层优化：JSON→SQLite | backend-dev | P0 |
| 阶段一 | 状态管理改进：会话封装 | backend-dev | P0 |
| 阶段二 | 错误处理统一化 | backend-dev | P1 |
| 阶段二 | 前端请求拦截增强 | frontend-dev | P1 |
| 阶段二 | 安全性加固 | backend-dev | P1 |
| 阶段三 | 日志系统统一 | backend-dev | P2 |
| 阶段三 | UI细节优化 | ui-designer | P2 |

---

## 二、团队成员职责

### 2.1 backend-dev (后端开发Agent)
- 数据层优化（JSON→SQLite）
- 状态管理改进
- 错误处理统一化
- 安全性加固
- 日志系统统一
- 代码审查（后端部分）

### 2.2 frontend-dev (前端开发Agent)
- 前端请求拦截增强（统一axios拦截器）
- API调用规范化
- 前端状态管理增强
- 前端代码审查

### 2.3 ui-designer (UI设计师Agent)
- 主题系统完善
- 深色模式CSS变量覆盖
- 错误状态UI设计规范
- 动效规范统一

### 2.4 code-reviewer (代码审查Agent)
- 全局代码审查
- 安全隐患检测
- 代码质量评估
- 优化建议输出

---

## 三、详细任务分解

### 3.1 backend-dev 任务

#### Task 1: 数据层优化 (P0)
```
文件：backend/services/data_service.py (新建)
     backend/api/works.py (修改)
     backend/core/config.py (修改)

内容：
1. 创建SQLite数据库服务
2. 迁移现有JSON数据到SQLite
3. 提供Work CRUD接口
4. 保持向后兼容（配置文件仍用JSON）

数据库表设计：
works(id, title, inspiration, phase, created_at, updated_at)
parts(id, work_id, part_num, content, summary, status)
characters(id, work_id, name, role, traits, ...)
```

#### Task 2: 状态管理改进 (P0)
```
文件：backend/services/writing_session.py (新建)
     backend/services/writing_service.py (修改)

内容：
1. 创建WritingSession类封装会话状态
2. 使用上下文管理器管理生命周期
3. 添加会话超时自动清理
4. 支持多作品并发创作
```

#### Task 3: 错误处理统一化 (P1)
```
文件：backend/core/exceptions.py (新建)
     backend/core/error_handler.py (修改)
     各API路由 (修改)

内容：
1. 定义统一异常类（APIError, NetworkError, LLMError等）
2. 定义统一错误码（ErrorCode枚举）
3. 在main.py添加全局异常处理器
4. 统一API错误响应格式
```

#### Task 4: 安全性加固 (P1)
```
文件：backend/main.py (修改)
     backend/core/config.py (修改)

内容：
1. CORS限制到已知域名
2. 添加简单token认证（可选）
3. API Key加密存储
4. 添加请求限流
```

#### Task 5: 日志系统统一 (P2)
```
文件：backend/core/logger.py (新建)
     backend/main.py (修改)

内容：
1. 统一logging配置
2. 添加日志文件轮转
3. 替换所有print()为logging
4. 添加结构化日志输出
```

---

### 3.2 frontend-dev 任务

#### Task 1: 请求拦截增强 (P1)
```
文件：frontend/src/api/index.js (修改)

内容：
1. 添加请求重试机制（最多3次）
2. 添加AbortController支持
3. 添加请求超时统一处理
4. 统一错误提示
```

#### Task 2: 状态管理增强 (P2)
```
文件：frontend/src/stores/works.js (修改)
     frontend/src/stores/writing.js (新建)

内容：
1. 添加works store的持久化
2. 创建writing store管理创作状态
3. 添加状态同步机制
```

#### Task 3: API调用规范化 (P2)
```
文件：frontend/src/api/index.js (修改)
     frontend/src/views/*.vue (修改)

内容：
1. 检查并替换所有直接使用axios的地方
2. 统一使用api实例
3. 添加TypeScript类型定义（如果使用TS）
```

---

### 3.3 ui-designer 任务

#### Task 1: 深色模式完善 (P0)
```
文件：frontend/src/styles/variables.css (修改)
     frontend/src/styles/dark-theme.css (新建)

内容：
1. 完善深色模式CSS变量
2. 覆盖所有Naive UI组件变量
3. 添加Modal、Dropdown、Tooltip等组件适配
```

#### Task 2: 错误状态UI设计 (P0)
```
文件：frontend/src/components/ErrorDisplay.vue (新建)
     frontend/src/views/WritingProgress.vue (修改)

内容：
1. 创建统一错误展示组件
2. 5种错误类型设计（网络断开/API超时/内存不足/文件保存失败/认证过期）
3. 错误弹窗规范
```

#### Task 3: 动效规范统一 (P1)
```
文件：frontend/src/styles/animations.css (修改)
     全局样式文件 (修改)

内容：
1. 统一时长标准（fast/normal/slow）
2. 统一缓动函数
3. 添加prefers-reduced-motion适配
4. 卡片/按钮/进度条动效规范
```

---

### 3.4 code-reviewer 任务

#### Task 1: 全局代码审查
```
输出：代码审查报告.md

审查内容：
1. 安全隐患检测
2. 代码重复检测
3. 性能问题检测
4. 代码规范检查
```

---

## 四、执行计划

### 第一阶段（Day 1）
- backend-dev: 开始数据层优化
- frontend-dev: 开始请求拦截增强
- ui-designer: 开始深色模式完善

### 第二阶段（Day 2）
- backend-dev: 完成数据层优化，开始状态管理
- frontend-dev: 继续前端优化
- code-reviewer: 开始全局代码审查

### 第三阶段（Day 3）
- backend-dev: 完成状态管理和错误处理
- ui-designer: 完成错误状态UI设计
- 汇总审查报告

---

## 五、沟通机制

1. **每日站会**：各Agent汇报进度
2. **代码提交**：通过文件系统同步
3. **问题升级**：遇到阻塞问题时立即汇报

---

## 六、交付物

1. 架构审阅报告（已完成）
2. 团队协作方案（本文档）
3. 各Agent优化后的代码文件
4. 代码审查报告
5. 最终测试报告
