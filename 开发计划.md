# 番茄小说AI创作系统 V5 开发计划

## 项目概述

将 V4 命令行版本升级为桌面 GUI 应用，采用 FastAPI + Vue 3 + PyInstaller 技术栈，实现可视化操作、多作品管理和实时进度展示。

## 技术架构

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    桌面应用入口                          │
│              (main_gui.py / PyInstaller)               │
├─────────────────────────────────────────────────────────┤
│  前端层 (Vue 3 + Vite + Naive UI)                      │
│  - 左侧图标导航栏 + 右侧内容区                           │
│  - SSE 实时接收后端推送                                  │
├─────────────────────────────────────────────────────────┤
│  后端层 (FastAPI)                                      │
│  - REST API: 作品管理/配置管理/创作控制                   │
│  - SSE: 实时日志/进度推送                                │
│  - WebSocket: AI辅助改写（可选）                         │
├─────────────────────────────────────────────────────────┤

│  核心层 (复用 V4)                                       │
│  - agents/: 10个Agent                                   │
│  - llm_client.py: LLM调用                               │
│  - config.py: 配置管理                                  │
│  - story_state.py: 状态管理                             │
│  - cost_tracker.py: 成本统计                            │
└─────────────────────────────────────────────────────────┘
```

### 目录结构

```
番茄小说AI创作系统_V5/
├── frontend/                 # Vue 3 前端
│   ├── src/
│   │   ├── components/      # 组件
│   │   ├── views/           # 页面视图
│   │   ├── stores/          # Pinia 状态管理
│   │   ├── api/             # API 调用
│   │   └── utils/           # 工具函数
│   ├── index.html
│   ├── vite.config.ts
│   └── package.json
├── backend/                 # FastAPI 后端
│   ├── main.py              # 主入口
│   ├── api/                 # API 路由
│   │   ├── works.py         # 作品管理
│   │   ├── config.py        # 配置管理
│   │   ├──创作.py           # 创作控制
│   │   └── rewrite.py       # AI改写
│   ├── sse.py               # SSE 事件
│   └── services/            # 业务服务
├── core/                    # 核心逻辑（V4迁移）
│   ├── agents/              # Agent模块
│   ├── llm_client.py
│   ├── config.py
│   ├── story_state.py
│   ├── cost_tracker.py
│   └── ...
├── main_gui.py              # 桌面入口
├── .env                     # 环境配置
└── requirements.txt
```

## 功能模块

### 1. 启动器模块 (main_gui.py)

- 检测 Python 环境
- 检测/安装 Node.js（如需要）
- 启动 FastAPI 后端
- 启动 Vue 开发服务器
- 自动打开浏览器
- 支持 CLI 参数（--resume 等）

### 2. 后端 API 模块

#### 作品管理 API
- `GET /api/works` - 获取作品列表
- `POST /api/works` - 创建新作品
- `GET /api/works/{id}` - 获取作品详情
- `PUT /api/works/{id}` - 更新作品
- `DELETE /api/works/{id}` - 删除作品

#### 配置管理 API
- `GET /api/config` - 获取全局配置
- `PUT /api/config` - 更新配置（自动写入 .env）
- `GET /api/config/templates` - 获取预设模板列表

#### 创作控制 API
- `POST /api/create/start` - 开始创作
- `POST /api/create/pause` - 暂停创作
- `POST /api/create/resume` - 继续创作
- `POST /api/create/confirm` - 确认继续（手动模式）
- `GET /api/create/status` - 获取创作状态

#### AI 改写 API
- `POST /api/rewrite` - AI 改写指定文字

#### SSE 事件
- `GET /api/events/logs` - 实时日志
- `GET /api/events/progress` - 进度更新
- `GET /api/events/scores` - 评分更新

### 3. 前端页面

#### 首页/作品列表
- 作品卡片展示
- 新建作品按钮
- 作品搜索/筛选

#### 配置页面
- 预设模板选择（短篇1万/中篇5万/长篇10万/自定义）
- 展开式 Agent 配置（每个 Agent 单独设置模型、温度、max_tokens）
- 确认模式开关（自动/手动）
- LLM 配置面板（模型选择、API Key、Base URL）

#### 创作页面
- Phase 进度条
- Agent 调用链可视化
- 实时日志面板（可滚动）
- 评分仪表盘（逻辑/情感/一致性）
- Part 列表和预览

#### 预览页面
- Part 切换标签
- 文本编辑器（可编辑）
- 一键优化按钮（重新调用 StyleOptimizer）
- AI 辅助改写（选中文字 → 浮动气泡）

#### 报告页面
- 字数统计图表
- 评分曲线
- P0/P1 问题列表
- 成本统计（Token 消耗、费用）

### 4. 数据存储

- SQLite: 作品元数据、配置、创作状态
- JSON: 作品内容、Agent 输出
- .env: LLM 配置

### 5. 跨语言调用

前端配置 LLM → POST /api/config → 后端写入 .env → 重载 config.py

## 开发阶段

### 阶段一：基础架构（预计 2 小时）

1. 创建 V5 目录结构
2. 搭建 FastAPI 后端框架
3. 搭建 Vue 3 前端框架
4. 配置 Vite 开发服务器
5. 实现前后端联调基础

### 阶段二：后端核心（预计 3 小时）

1. 实现作品管理 CRUD
2. 实现配置管理（读写 .env）
3. 集成 V4 核心模块（agents, llm_client）
4. 实现创作流程编排
5. 实现 SSE 实时推送

### 阶段三：前端核心（预计 4 小时）

1. 实现左侧图标导航
2. 实现作品列表页
3. 实现配置面板（模板 + Agent 展开配置）
4. 实现创作页面（进度 + 日志 + 评分）

### 阶段四：高级功能（预计 3 小时）

1. 实现预览页面（文本编辑 + Part 切换）
2. 实现 AI 辅助改写（浮动气泡）
3. 实现报告页面
4. 实现新手引导

### 阶段五：打包发布（预计 1 小时）

1. 配置 PyInstaller 打包
2. 测试桌面启动
3. 修复兼容性问题
4. 生成 .exe 文件

## 关键实现细节

### SSE 实现

```python
# backend/sse.py
from fastapi import APIRouter, Request
from fastapi.responses import StreamingResponse
import asyncio
import json

router = APIRouter()

async def event_generator(request: Request, event_type: str):
    queue = asyncio.Queue()
    # 注册到全局事件队列
    EVENTS[event_type].append(queue)
    
    try:
        while True:
            if await request.is_disconnected():
                break
            data = await queue.get()
            yield f"data: {json.dumps(data)}\n\n"
            await asyncio.sleep(0.1)
    finally:
        EVENTS[event_type].remove(queue)

@router.get("/events/logs")
async def logs(request: Request):
    return StreamingResponse(event_generator(request, "logs"))
```

### 前端配置写入 .env

```python
# backend/api/config.py
@router.put("/config")
async def update_config(config: Config):
    # 写入 .env 文件
    env_path = Path(__file__).parent.parent / ".env"
    lines = []
    if env_path.exists():
        lines = env_path.read_text(encoding='utf-8').splitlines()
    
    # 更新配置
    config_dict = config.model_dump()
    # ... 替换或追加配置行
    
    env_path.write_text('\n'.join(lines), encoding='utf-8')
    
    # 重载 config 模块
    import importlib
    importlib.reload(core.config)
    
    return {"status": "ok"}
```

### 浮动气泡 AI 改写

```vue
<!-- 前端实现 -->
<template>
  <div @mouseup="showPopup">
    <slot></slot>
    <Teleport to="body">
      <div v-if="visible" class="popup" :style="position">
        <button @click="rewrite('polish')">润色</button>
        <button @click="rewrite('expand')">扩写</button>
        <button @click="rewrite('compress')">缩写</button>
      </div>
    </Teleport>
  </div>
</template>
```

## 依赖清单

### Python 依赖
- fastapi
- uvicorn
- sse-starlette
- python-multipart
- pydantic
- python-dotenv
- (V4 原有依赖)

### Node.js 依赖
- vue: ^3.4
- vite: ^5.0
- naive-ui: ^2.38
- pinia: ^2.1
- @vueuse/core
- axios

## 风险与对策

| 风险 | 对策 |
|---|---|
| V4 Agent 与前端状态同步复杂 | 使用 V4 的 story_state 作为唯一真相源 |
| SSE 连接不稳定 | 添加重连机制和心跳检测 |
| 大文本编辑卡顿 | 使用虚拟滚动或分片加载 |
| 跨域问题 | FastAPI 配置 CORS |
