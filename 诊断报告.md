# 番茄小说AI创作系统 V5 - 诊断报告

**诊断时间**: 2026-03-29 15:52
**问题**: 创作工作流在 Part 2 卡住

## 当前状态

### 已完成的修复（阶段一优化）
1. ✅ WritingService 4个Phase方法已接通
2. ✅ 添加了 asyncio.to_thread 包装避免事件循环阻塞
3. ✅ 修复了字典key类型问题（统一使用字符串）
4. ✅ 添加了详细的诊断日志

### 工作流执行日志分析
从用户提供的截图可以看到：
- ✅ **Phase 1 完成**: 灵感解析成功
- ✅ **Phase 2 完成**: 情节规划成功
- ✅ **Part 1 完成**: 生成 1000+ 字内容
- ⚠️ **Part 2 开始**: 日志显示"开始创作 Part 2/3"后卡住

## 问题诊断

### 配置分析
- **PART_WORD_MIN = 2500** 字
- **PART_WORD_MAX = 4000** 字
- **Part 1 实际生成**: 1000+ 字（不足最小值 2500）

### 可能的原因

1. **上下文过长导致超时**
   - Part 1 生成后，Part 2 的上下文包含了 Part 1 的全部内容
   - 如果 Part 1 有 1000+ 字，Part 2 的 prompt 可能超过 2000-3000 字符
   - MiniMax API 可能响应较慢或超时

2. **API 调用卡住**
   - call_llm 虽然有 3 次重试机制，但没有设置超时时间
   - 可能网络请求一直等待响应

3. **Token 限制**
   - 上下文 + 新生成内容可能超过模型最大 Token 限制
   - 但没有触发异常，可能是 API 返回空或异常未被捕获

## 已添加的诊断日志

### PartWriterAgent 日志（part_writer_agent.py）
```python
- [PartWriterAgent] Part X 开始获取上下文...
- [PartWriterAgent] Part X 上下文长度: N 字符
- [PartWriterAgent] Part X 目标字数: N, 上限: N
- [PartWriterAgent] Part X 准备调用LLM...
- [PartWriterAgent] Part X 调用call_llm...
- [PartWriterAgent] Part X call_llm完成，耗时: N.NN秒
- [PartWriterAgent] Part X 生成字数: N
```

### LLM Client 日志（llm_client.py）
```python
- [LLM] 第N次调用开始: model=X, max_tokens=N, agent=X
- [LLM] messages长度: N 条, 系统提示长度: N 字符
- [LLM] API调用成功，耗时: N.NNms
- [LLM] 返回内容长度: N 字符, 预览: ...
- [LLM] Token使用: prompt=N, completion=N, total=N
- [LLM] 警告: usage为None，无法记录Token消耗
- [LLM] 第N次调用失败: X
- [LLM] 等待N秒后重试...
- [LLM] 3次调用全部失败，抛出异常
```

## 下一步调试步骤

### 步骤 1: 重启服务并观察日志

1. **停止当前服务**
   ```bash
   # 在 backend 目录下
   Ctrl+C 停止服务器
   ```

2. **重新启动后端**
   ```bash
   cd backend
   python main.py
   ```

3. **清空浏览器控制台**
   - F12 打开开发者工具
   - Console 标签页右键 -> Clear console

4. **重新开始创作**
   - 在浏览器中点击"开始创作"
   - 观察三个地方的日志：
     - **浏览器控制台**（前端日志）
     - **后端终端**（backend main.py 的输出）
     - **页面日志面板**（页面右侧的日志）

### 步骤 2: 分析 Part 2 的日志

重点关注以下内容：

1. **上下文长度**
   ```
   [PartWriterAgent] Part 2 上下文长度: XXXX 字符
   ```
   - 如果超过 3000-4000 字符，可能过长

2. **API 调用耗时**
   ```
   [PartWriterAgent] Part 2 call_llm完成，耗时: XX.XX秒
   ```
   - 如果超过 30-60 秒，可能超时

3. **Token 使用情况**
   ```
   [LLM] Token使用: prompt=XXXX, completion=XXXX, total=XXXX
   ```
   - MiniMax-Text-01 最大 Token 约 4000-8000
   - 如果 total 接近上限，可能触发限制

4. **API 调用失败**
   ```
   [LLM] 第X次调用失败: ...
   ```
   - 查看具体错误信息

### 步骤 3: 检查生成的作品文件

在创作卡住时，检查作品数据文件：

```bash
cd data/works
ls -lt  # 查看最新的作品文件
cat <work_id>.json | python -m json.tool | head -100
```

检查：
- `parts` 字段是否有 Part 1 的内容
- `part_summaries` 字段是否有摘要
- `phase` 字段显示当前阶段

### 步骤 4: 如果 Part 2 一直卡住

如果 Part 2 确实卡住了（没有完成日志），尝试：

1. **添加超时机制**（修改 llm_client.py）
   ```python
   response = client.chat.completions.create(
       model=model_name,
       messages=messages,
       temperature=temp,
       max_tokens=max_tokens,
       timeout=60.0,  # 添加超时：60秒
   )
   ```

2. **减少 Part 字数**（临时测试）
   修改 `backend/core/config.py`：
   ```python
   _PART_WORD_MIN = 800  # 从 2500 降低
   _PART_WORD_MAX = 1200  # 从 4000 降低
   ```

3. **限制上下文长度**（修改 part_writer_agent.py）
   在 `get_part_context` 中只保留最近 1-2 个 Part 的摘要，而不是全部

### 步骤 5: 捕获更详细的异常信息

在 `writing_service.py` 的 `_phase3_writing` 方法中，捕获并记录完整异常：

```python
except Exception as e:
    print(f"[PartWriterAgent] Part {i} 生成异常: {e}")
    import traceback
    traceback.print_exc()  # 打印完整堆栈
    self.log_error(str(e))
    return f"[Part {i}生成失败: {e}]"
```

## 预期正常日志

如果一切正常，你应该看到：

```
[WritingService] 进入Phase 3: 逐Part创作
[PartWriterAgent] Part 1 开始获取上下文...
[PartWriterAgent] Part 1 上下文长度: 1500 字符
[PartWriterAgent] Part 1 目标字数: 3000, 上限: 4200
[PartWriterAgent] Part 1 准备调用LLM...
[PartWriterAgent] Part 1 调用call_llm...
    [LLM] 第1次调用开始: model=MiniMax-Text-01, max_tokens=4700, agent=Part写作Agent
    [LLM] messages长度: 2 条, 系统提示长度: 2500 字符
    [LLM] API调用成功，耗时: 15234.56ms
    [LLM] 返回内容长度: 3200 字符, 预览: 第一句话...  
    [LLM] Token使用: prompt=1800, completion=2800, total=4600
[PartWriterAgent] Part 1 call_llm完成，耗时: 15.23秒
[PartWriterAgent] Part 1 生成字数: 3200
[PartWriterAgent] Part 1「开篇」 字数: 3200字 (目标: 3000)

[PartWriterAgent] Part 2 开始获取上下文...
[PartWriterAgent] Part 2 上下文长度: 4500 字符  <- 注意这个会变长
[PartWriterAgent] Part 2 目标字数: 3000, 上限: 4200
[PartWriterAgent] Part 2 准备调用LLM...
[PartWriterAgent] Part 2 调用call_llm...
    [LLM] 第1次调用开始: model=MiniMax-Text-01, max_tokens=4700, agent=Part写作Agent
    [LLM] messages长度: 2 条, 系统提示长度: 2500 字符
    [LLM] API调用成功，耗时: 20345.67ms
    [LLM] 返回内容长度: 3100 字符, 预览: 第一句话...
    [LLM] Token使用: prompt=3500, completion=2700, total=6200
[PartWriterAgent] Part 2 call_llm完成，耗时: 20.35秒
[PartWriterAgent] Part 2 生成字数: 3100
[PartWriterAgent] Part 2「发展」 字数: 3100字 (目标: 3000)
```

## 常见问题与解决

### 问题 1: 上下文过长导致 Token 超限
**症状**: `[LLM] Token使用: prompt=6500, completion=0, total=6500` 然后失败
**解决**: 
- 减少 `PART_WORD_MIN` 和 `PART_WORD_MAX`
- 在 `get_part_context` 中只保留最近 2 个 Part 的摘要

### 问题 2: API 响应超时
**症状**: 卡在 `[LLM] 第1次调用开始...` 很久（>60秒）
**解决**: 
- 添加 `timeout=60.0` 参数
- 检查网络连接
- 尝试切换供应商（如切换到 SiliconFlow）

### 问题 3: 生成字数严重不足
**症状**: `Part X只有1200字，严重不足（最小要求: 1750）`
**解决**: 
- 检查 Prompt 是否明确要求字数
- 在 Prompt 中强调字数要求
- 降低 `PART_WORD_MIN` 到更合理的值（如 1500）

### 问题 4: usage 为 None
**症状**: `[LLM] 警告: usage为None，无法记录Token消耗`
**解决**: 
- 某些 API 或旧版本模型不返回 usage 信息
- 不影响生成，可以忽略
- 如果需要精确计费，升级 API 或使用支持的模型

## 建议的长期优化

1. **降低 Part 字数要求**
   ```python
   # backend/core/config.py
   _PART_WORD_MIN = 1500  # 从 2500 降低
   _PART_WORD_MAX = 2500  # 从 4000 降低
   ```

2. **添加上下文长度限制**
   在 `get_part_context` 中只保留最近 2 个 Part 的完整内容和更早 Part 的摘要

3. **添加请求超时**
   ```python
   # backend/core/llm_client.py
   response = client.chat.completions.create(
       model=model_name,
       messages=messages,
       temperature=temp,
       max_tokens=max_tokens,
       timeout=60.0,  # 60秒超时
   )
   ```

4. **实现流式生成**
   使用 SSE 流式输出，避免用户以为系统卡死

5. **添加上下文压缩**
   对过长的上下文进行摘要压缩，减少 Token 消耗

## 总结

当前工作流已经打通，核心问题是 Part 2 的 LLM 调用可能由于以下原因卡住：
1. 上下文过长（4500+ 字符）
2. API 响应慢或超时
3. Token 接近上限
4. 网络问题

**下一步**: 重启服务，观察 Part 2 的详细日志，根据日志信息采取相应措施。
