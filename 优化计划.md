# 番茄小说AI创作系统 V5 优化计划

> 制定日期：2026-03-29
> 依据：V5产品审查报告（2026-03-29）
> 目标：将V5从35%完成度提升到可上线状态

---

## 一、问题总览

根据审查报告，V5存在 **10个P0/P1问题** 和 **15个P2问题**，核心矛盾是「前端壳子完成、核心管道未通」。

### 各模块完成度

| 模块 | 完成度 | 核心问题 |
|------|--------|----------|
| 前端 | 75% | 3个P0交互闭环未完成 |
| 后端API | 70% | WritingService全部是占位符 |
| 核心层 | 85% | Agent代码完整但未被调用 |
| 基础设施 | 80% | 打包配置需修复 |

---

## 二、阶段一：打通核心管道（4-6小时）⭐优先级最高

**目标：让V5从"空壳演示"变成"真正可用的产品"**

### 1.1 修复导入路径问题（B-02）⭐P0
**问题**：`config_api.py` 第106行使用 `import backend.core.llm_client` 绝对路径，在uvicorn运行时 `backend` 不在sys.path
**修复**：改为相对导入 `from core import llm_client`
**文件**：`backend/routes/config_api.py` 第106、159行

### 1.2 修复暂停参数不匹配（B-04）⭐P0
**问题**：`writing.py` 的 `pause_writing()` 使用GET参数 `work_id`，前端发送的是POST body `{"work_id": "xxx"}`
**修复**：统一为POST body格式
**文件**：`backend/routes/writing.py` 第44行

### 1.3 接通WritingService Phase方法（B-01）⭐⭐P0 最关键
**问题**：4个Phase方法全是 `asyncio.sleep(0.5)` 占位，核心创作逻辑未接入
**修复工作量**：中等

需要将V4 CLI中的创作主循环逻辑迁移到WritingService：

```python
# 当前状态（占位）
async def _phase1_planning(self): await asyncio.sleep(0.5)

# 目标状态（接入Agent）
async def _phase1_planning(self):
    agent = PlanningAgent(self.config)
    result = await agent.run(self.story_state)
    self.emitter.emit_phase1_planning(result)
```

**每个Phase需要接入的Agent**：
- Phase1 规划 → `PlanningAgent`（灵感解析、题材判断）
- Phase2 大纲 → `OutlineAgent`（情节规划）
- Phase3 创作 → `WritingAgent`（Part创作、字数约束）
- Phase4 优化 → `OptimizationAgent`（逻辑校验、情感评估、一致性检查、风格优化）

**文件**：`backend/services/writing_service.py` 第94-161行

### 1.4 接通CLI创作主循环（C-03）⭐P0
**问题**：`cli.py` 的 `main()` 只做了初始化，没有调用任何Agent执行创作流程
**修复**：补全V4风格的完整创作主循环
**文件**：`backend/cli.py` 第33-64行

### 1.5 修复PreviewEdit改写回写（F-02）⭐P0
**问题**：`doRewrite()` 拿到改写结果后没有替换原文
**修复**：用DOM操作替换 `contenteditable` 中的选中文本
**文件**：`frontend/src/views/PreviewEdit.vue` 第104-112行

---

## 三、阶段二：完善交互闭环（3-4小时）

### 2.1 实现手动确认功能（F-01）⭐P1
**问题**：`WritingProgress.vue` 的 `confirmAction()` 是空函数，手动确认模式不工作
**修复**：
1. 后端增加confirm事件处理接口
2. 前端确认后发送结果到后端
**文件**：`frontend/src/views/WritingProgress.vue` 第176-179行

### 2.2 SSE按work_id隔离（B-05/F-09）⭐P1
**问题**：SSE是全局广播，多作品同时创作时会混乱
**修复**：emitter支持按作品过滤事件
**文件**：`backend/services/sse.py` 第61行

### 2.3 实现reOptimize功能（F-03）⭐P1
**问题**：`PreviewEdit.vue` 的 `reOptimize()` 是空函数
**修复**：接入后端优化API
**文件**：`frontend/src/views/PreviewEdit.vue` 第114-116行

### 2.4 统一前端HTTP拦截器（F-08）⭐P2
**问题**：每个页面各自创建axios实例，没有统一拦截器
**修复**：创建 `utils/request.js`，统一token、错误处理
**文件**：多处

### 2.5 接入成本追踪到WritingService
**问题**：Token成本追踪代码存在但WritingService未调用
**修复**：在创作流程中记录实际Token消耗

---

## 四、阶段三：质量提升（2-3小时）

### 3.1 修复Collapse组件交互（F-04）⭐P1
**问题**：`ConfigPanel.vue` 使用 `accordion` 模式，一次只能展开一个Agent配置
**修复**：改为 `accordion={false}` 或使用多个独立Collapse
**文件**：`frontend/src/components/ConfigPanel.vue` 第97行

### 3.2 修复Report组件引用（F-05）⭐P1
**问题**：`PreviewEdit.vue` 嵌套使用Report组件，引用路径未确认
**修复**：验证 `<report-view :work-id="workId" />` 路径正确性

### 3.3 修复PyInstaller打包配置（I-01）⭐P1
**问题**：Analysis入口是 `launcher.py`，但打包后不会从backend/启动uvicorn
**修复**：改为 `backend/main.py` 或创建打包专用启动脚本
**文件**：`build.py` 第24行

### 3.4 修复requests依赖缺失（I-02）⭐P1
**问题**：`launcher.py` 使用 `requests.get` 做健康检查，但requests不在requirements.txt
**修复**：将requests加入requirements.txt或改用httpx
**文件**：`requirements.txt`、`launcher.py` 第52行

### 3.5 增强用户体验（P2）
| 问题 | 修复方案 |
|------|----------|
| F-06 无loading骨架屏 | 增加初始加载进度指示 |
| F-07 删除无二次确认 | WorksList增加确认弹窗 |
| F-10 侧边栏高亮错误 | 修复路由自动高亮逻辑 |

### 3.6 基础设施完善（P2）
| 问题 | 修复方案 |
|------|----------|
| I-03 无静态文件托管 | 生产环境配置 `app.mount("/", StaticFiles(...))` |
| I-04 版本号不统一 | 统一管理在 `__version__` 常量 |
| I-05 日志无限增长 | 增加日志自动清理机制 |

---

## 五、技术债清理（P2，建议后续迭代）

### 5.1 数据层增强（B-07）
**问题**：纯JSON文件存储，长篇性能堪忧
**建议**：
- 短期：增量更新JSON
- 长期：引入SQLite或TinyDB

### 5.2 安全性加固（B-08）
**问题**：无认证、无鉴权、API Key明文
**建议**：
- 添加简单token认证
- API Key加密存储
- 限制CORS范围

### 5.3 Prompt动态加载（C-01）
**问题**：Prompt变量在模块加载时缓存，不支持运行时更新
**修复**：增加缓存失效机制或每次重新加载

### 5.4 长上下文裁剪（C-02）
**问题**：`get_part_context()` 20 Part时上下文超长
**修复**：增加token数控制或LRU裁剪

### 5.5 日志系统重构（C-04）
**问题**：`base_agent.py` 使用print()，GUI模式下输出到后端控制台
**修复**：通过SSE推送或使用Python logging

### 5.6 重复代码清理（C-05）
**问题**：`llm_client.py` 中函数定义重复
**修复**：删除第189-224行的重复定义

---

## 六、依赖关系图

```
阶段一（核心管道）
    │
    ├── 1.1 修复导入路径 ─────────────┐
    ├── 1.2 修复暂停参数 ─────────────┤
    ├── 1.3 WritingService接入Agent ──┼──┐
    └── 1.4 CLI主循环接通 ────────────┘  │
                                        │
阶段二（交互闭环）<───────────────────────┘
    │
    ├── 2.1 confirmAction实现
    ├── 2.2 SSE按work_id隔离
    ├── 2.3 reOptimize实现
    ├── 2.4 HTTP拦截器
    └── 2.5 成本追踪接入

阶段三（质量提升）
    │
    ├── 3.1-3.4 P1问题修复
    └── 3.5-3.6 P2用户体验

后续迭代
    │
    └── 技术债清理
```

---

## 七、建议开发顺序

1. **第一周**：阶段一全部 + 1.5 PreviewEdit改写
2. **第二周**：阶段二全部
3. **第三周**：阶段三P1问题
4. **第四周**：阶段三P2问题 + 技术债评估

**关键里程碑**：阶段一完成后，V5即可从"演示空壳"变为"基本可用产品"

---

## 八、资源评估

| 阶段 | 预计工时 | 难度 |
|------|----------|------|
| 阶段一 | 4-6小时 | 中等 |
| 阶段二 | 3-4小时 | 中低 |
| 阶段三 | 2-3小时 | 低 |
| 技术债 | 后续迭代 | 不等 |
| **总计** | **9-13小时** | - |

---

*本计划基于2026-03-29 V5产品审查报告制定*
