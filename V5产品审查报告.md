# 番茄小说AI创作系统 V5 - 产品审查报告

> 审查日期：2026-03-29
> 审查范围：V5 完整代码库（前后端 + Agent核心层）
> 项目定位：V4 命令行版本 → GUI 桌面应用升级

---

## 一、整体评价

V5 是从 V4 命令行工具向 GUI 桌面应用的架构升级。技术选型合理（FastAPI + Vue 3 + Naive UI），前后端分离清晰，API 设计规范。多供应商 LLM 支持、Prompt 外部化、SSE 实时推送等核心能力设计完善。

**但是，V5 目前处于「前端壳子完成、核心管道未通」的状态。** GUI 的全部 6 个页面和 5 个 API 模块虽然都搭建了，但后端的创作流程服务（writing_service.py）全部是 `asyncio.sleep` 占位，前端与 Agent 核心之间没有真正接通。CLI 入口（cli.py）也只有 V4 的导入但没有实际的创作主循环逻辑。

简而言之：**V5 是一个完成度约 35-40% 的半成品。** 外观和交互框架已就位，但核心功能管道（前端→API→WritingService→Agent 调用链）完全断开。

---

## 二、各模块审查

### 2.1 前端 - 完成度 75%

**做得好的部分：**
- 6 个页面布局合理，番茄红主题统一，视觉风格一致
- Naive UI 组件使用规范（Modal、Form、Tabs、Collapse、Slider 等）
- SSE 实时事件接收逻辑正确（EventSource + JSON 解析 + 分类处理）
- 新手引导覆盖 4 步核心流程，localStorage 持久化
- 作品 CRUD 操作通过 Pinia Store 统一管理
- 路由设计清晰，Hash 模式适合桌面应用

**问题清单：**

| 编号 | 严重度 | 问题 | 位置 |
|------|--------|------|------|
| F-01 | P0 | `WritingProgress.vue` 的 `confirmAction()` 是空函数（TODO 注释），手动确认模式完全不工作 | 第 176-179 行 |
| F-02 | P0 | `PreviewEdit.vue` 的 `doRewrite()` 拿到改写结果后没有替换原文（TODO 注释），选中文本改写功能不可用 | 第 104-112 行 |
| F-03 | P0 | `PreviewEdit.vue` 的 `reOptimize()` 是空函数，重新优化按钮无效 | 第 114-116 行 |
| F-04 | P1 | `ConfigPanel.vue` 使用 `accordion` 模式的 Collapse，用户一次只能展开一个 Agent 配置，对比查看不方便（但上次审查已修复了 `name` 绑定问题） | 第 97 行 |
| F-05 | P1 | `Report.vue` 在 PreviewEdit 页面作为 Modal 嵌套使用，但 Report 组件引用路径未确认是否正确（`<report-view :work-id="workId" />`） | 第 58 行 |
| F-06 | P1 | 前端没有任何 loading 骨架屏或进度指示器用于初始加载，用户在网络慢时看到空白 |
| F-07 | P2 | `WorksList.vue` 删除作品没有二次确认弹窗，容易误删 | 第 32 行 |
| F-08 | P2 | 所有页面各自创建 `axios.create({ baseURL: '/api' })` 实例，没有统一的 HTTP 拦截器（如 token、错误处理） | 多处 |
| F-09 | P2 | `WritingProgress.vue` 的 SSE 是全局广播（所有客户端共享），多作品同时创作时会混乱 | `sse.py` 全局单例 |
| F-10 | P2 | 侧边栏 Sidebar 没有根据当前路由自动高亮对应项（虽然传了 `current` prop，但每个页面是硬编码的） | Sidebar.vue |

### 2.2 后端 API 层 - 完成度 70%

**做得好的部分：**
- 5 个路由模块划分清晰：works / config_api / writing / rewrite / sse
- Pydantic 模型规范（BaseModel 验证请求体）
- CORS 中间件配置正确（开发环境）
- SSE 实现合理：asyncio.Queue + 心跳保活 + 事件分类

**问题清单：**

| 编号 | 严重度 | 问题 | 位置 |
|------|--------|------|------|
| B-01 | P0 | `writing_service.py` 的 4 个 Phase 方法全是 `asyncio.sleep(0.5)` 占位，核心创作逻辑未接入 | 第 94-161 行 |
| B-02 | P0 | `config_api.py` 第 106 行 `import backend.core.llm_client` 使用了绝对路径导入，实际应该用相对导入 `from core import llm_client`（在 uvicorn 运行时 `backend` 不在 sys.path） | 第 106、159 行 |
| B-03 | P1 | `WritingService._save()` 每次操作都完整重写 JSON 文件，没有增量更新。在高频写入场景（逐 Part 创作）下存在性能和原子性问题 | 第 172-174 行 |
| B-04 | P1 | `writing.py` 的 `pause_writing()` 使用 GET 参数接收 `work_id`（`def pause_writing(work_id: str)`），但前端发送的是 POST body `{"work_id": "xxx"}`，参数名不匹配，暂停功能不工作 | 第 44 行 |
| B-05 | P1 | SSE emitter 是全局单例，没有按 `work_id` 隔离事件。如果两个作品同时创作，A 的进度事件会推送给 B 的客户端 | `sse.py` 第 61 行 |
| B-06 | P1 | `works.py` 的 `list_works()` 使用 `except: continue` 静默吞掉所有异常，损坏的 JSON 文件会被无声跳过 | 第 46 行 |
| B-07 | P2 | 作品数据存储为单个大 JSON 文件（`data/works/{id}.json`），长篇小说（10万字 + 多 Part）的 JSON 可能超过 10MB，读写性能堪忧 | `works.py` |
| B-08 | P2 | 没有 API 认证/鉴权机制，任何能访问端口的人都可以操作所有作品和修改 API Key | 全局 |
| B-09 | P2 | `rewrite_service.py` 调用 `get_client()` 获取全局客户端，没有使用 `_get_client_for_agent()` 来支持分供应商配置 | 第 52 行 |

### 2.3 核心层（Agents + LLM） - 完成度 85%

**做得好的部分：**
- 9 个 Agent 代码完整，每个都有系统 Prompt + 用户 Prompt + 结果解析 + 错误降级
- `llm_client.py` 非常健壮：思维链过滤、JSON 4 级修复、response_format 自动降级、temperature 安全修正、3 次重试、Token 消耗记录
- 多供应商支持（MiniMax/DeepSeek/OpenAI/SiliconFlow），全局模式和 per-agent 独立模式
- Prompt 外部化到 `prompts/` 目录，支持变量占位符替换
- 故事状态管理完善：8 层上下文传递、角色状态快照、关键事实清单
- 成本追踪器按模型分别计费

**问题清单：**

| 编号 | 严重度 | 问题 | 位置 |
|------|--------|------|------|
| C-01 | P1 | `prompt_loader.py` 的 `_PROMPT_VARS` 在模块加载时从 `config` 读取常量，如果用户在前端改了模板后不重启后端，Prompt 中的变量值不会更新 | 第 13-18 行 |
| C-02 | P1 | `story_state.py` 的 `get_part_context()` 构建 8 层上下文，长篇（20 Part）时上下文可能超长，没有做 token 数控制或裁剪 | 第 88-169 行 |
| C-03 | P1 | `cli.py` 的 `main()` 函数只做了初始化和状态保存，没有调用任何 Agent 执行创作流程，CLI 模式完全不工作 | 第 33-64 行 |
| C-04 | P2 | `base_agent.py` 的日志方法还是 `print()`，在 GUI 模式下这些输出到了后端控制台而不是通过 SSE 推送到前端 | 第 28-35 行 |
| C-05 | P2 | `llm_client.py` 中 `_client` 和 `_json_client` 的函数定义在文件中出现了两次（第 19-30 行和第 189-224 行），后面的定义覆盖了前面的，造成冗余代码 | 第 19 vs 189 行 |

### 2.4 基础设施 - 完成度 80%

**做得好的部分：**
- `launcher.py` 自动检测环境、启动前后端、打开浏览器
- 安装脚本 `安装脚本.bat` 检查 Python/Node.js、安装依赖、创建 .env
- `启动图形界面.bat` 检查 .env 存在性和 API Key 配置
- PyInstaller 打包配置基本完整

**问题清单：**

| 编号 | 严重度 | 问题 | 位置 |
|------|--------|------|------|
| I-01 | P1 | `build.py` 的 Analysis 入口是 `launcher.py`，但 PyInstaller 打包后 `launcher.py` 不会从 backend/ 启动 uvicorn，需要改为 `backend/main.py` 或者打包一个启动脚本 | `build.py` 第 24 行 |
| I-02 | P1 | `launcher.py` 的 `start_backend()` 使用 `requests.get` 做健康检查，但 `requests` 不在 `requirements.txt` 中 | 第 52 行 |
| I-03 | P2 | 生产环境没有配置 FastAPI 静态文件托管（`app.mount("/", StaticFiles(...))`），前端 dist 构建后无法通过后端端口直接访问 | `main.py` |
| I-04 | P2 | 没有版本号统一管理（`package.json` 写 5.0.0，`main.py` 写 5.0.0，Sidebar 写硬编码 "V5"） | 多处 |
| I-05 | P2 | `data/logs/` 目录没有自动清理机制，多次创作后日志会无限增长 | `logger.py` |

---

## 三、核心断裂点分析

V5 当前最大的问题不是单个 Bug，而是 **核心功能管道未打通**。具体来说，存在以下三条断裂链：

### 断裂链 1：GUI 创作流程（最关键）

```
前端 WritingProgress.vue
  → POST /api/writing/start
    → writing.py (正确触发后台任务)
      → WritingService.run()
        → _phase1_planning()  ← 全部是 asyncio.sleep 占位
        → _phase2_outline()   ← 同上
        → _phase3_writing()   ← 同上
        → _phase4_optimize()  ← 同上
```

**现状**：点击"开始创作"后，前端会看到 4 个 Phase 快速闪过（每个 sleep 0.5 秒），然后跳转到报告页。但没有真正的 Agent 调用发生。

**修复工作量**：中等。需要将 V4 CLI 中的创作主循环逻辑（cli.py 中被截断的部分）迁移到 WritingService 的 4 个 Phase 方法中，每个 Phase 调用对应的 Agent，并通过 emitter 推送 SSE 事件。

### 断裂链 2：CLI 创作流程

```
cli.py main()
  → init_app_config()
  → StoryState(inspiration)
  → state.save()
  → 函数结束 ← 缺少创作主循环
```

**现状**：`python cli.py "灵感" --auto` 执行后只会打印配置信息，然后退出。V4 的完整创作主循环（调用 9 个 Agent 的串联逻辑）没有从 V4 迁移过来。

**修复工作量**：中等。需要在 `cli.py` 的 `main()` 中补全 V4 风格的创作主循环。

### 断裂链 3：前端交互闭环

```
PreviewEdit.vue doRewrite()
  → POST /api/rewrite/ (正确调用后端)
    → RewriteService.rewrite() (正确返回结果)
      → result.rewritten ← 拿到了改写结果
        → TODO: 替换原文 ← 没有执行
```

**现状**：AI 改写 API 本身是通的（`RewriteView.vue` 独立页面可以正常工作），但 `PreviewEdit.vue` 中的选中文本改写功能没有把结果回写到编辑器中。

**修复工作量**：小。只需要在 `doRewrite()` 中用 DOM 操作替换 contenteditable 中的选中文本。

---

## 四、架构设计评审

### 4.1 技术架构评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 技术选型 | 8/10 | FastAPI + Vue3 + Naive UI 组合成熟、轻量，适合桌面工具 |
| 前后端分离 | 7/10 | 分层清晰，但缺少统一的 HTTP 拦截器和错误处理 |
| API 设计 | 7/10 | RESTful 风格基本规范，部分接口设计不够一致（pause 用 GET 参数） |
| 状态管理 | 6/10 | 前端 Pinia 只有 works store，缺少 config、writing 等状态管理 |
| SSE 实时推送 | 6/10 | 设计合理但全局广播、无 work_id 隔离是硬伤 |
| 数据存储 | 5/10 | 纯 JSON 文件存储，没有数据库，长篇数据性能和并发安全有问题 |
| 安全性 | 3/10 | 无认证、无鉴权、.env 中 API Key 明文、CORS 全开 |

### 4.2 值得肯定的架构决策

1. **多供应商 LLM 支持**：从 V4 的单供应商升级为 4 供应商 + per-agent 独立选择，设计灵活
2. **Prompt 外部化**：8 个 Agent 的 Prompt 全部支持外部文件加载 + 变量替换，无需改代码即可调优
3. **SSE 事件系统**：9 种事件类型覆盖完整创作生命周期，前端接收逻辑正确
4. **保留 CLI 入口**：`cli.py` 保留 V4 兼容用法，方便高级用户使用

### 4.3 需要改进的架构问题

1. **数据层太薄**：JSON 文件存储无法支撑生产使用。建议至少引入 SQLite，或使用 TinyDB/JSONDB 等轻量方案
2. **SSE 需要按 work_id 隔离**：当前全局广播模式不适用于多作品场景
3. **前端缺少统一状态层**：当前每个页面各自管理 API 调用，没有统一的 config store 和 writing store
4. **后端缺少服务层抽象**：`WritingService` 既是编排器又是状态管理器，职责过重

---

## 五、与 V4 的功能对比

| 功能 | V4（CLI） | V5（GUI） | 状态 |
|------|-----------|-----------|------|
| 灵感解析 | 完整 | Agent 代码在，但 WritingService 未调用 | 管道断开 |
| 题材判断 | 完整 | 同上 | 管道断开 |
| 情节规划 | 完整 | 同上 | 管道断开 |
| Part 创作 + 字数约束 | 完整 | 同上 | 管道断开 |
| 逻辑校验 | 完整 | 同上 | 管道断开 |
| 情感评估 | 完整 | 同上 | 管道断开 |
| 一致性检查 | 完整 | 同上 | 管道断开 |
| 风格优化 | 完整 | 同上 | 管道断开 |
| P0 重写循环 | 完整 | 未迁移 | 缺失 |
| 字数补写循环 | 完整 | 未迁移 | 缺失 |
| 断点续写 | 完整 | 框架有，逻辑未通 | 管道断开 |
| 单 Part 重写 | 完整 | 框架有，逻辑未通 | 管道断开 |
| 多作品管理 | 无 | 完整 | 新增功能 |
| LLM 配置面板 | .env 手动编辑 | GUI 配置 | 新增功能 |
| Agent 级配置 | .env 手动编辑 | GUI 配置 | 新增功能 |
| 实时进度 | 终端输出 | SSE 推送 + 日志面板 | 新增功能 |
| 作品预览/编辑 | 无 | contenteditable 编辑器 | 新增功能 |
| AI 改写（选中文字） | 无 | 独立页面可用，预览页未通 | 部分完成 |
| 创作报告 | 终端打印 | GUI 报告页 | 新增功能 |
| 新手引导 | 无 | 4 步引导 | 新增功能 |
| 桌面打包 | 无 | PyInstaller 配置基本就绪 | 新增功能 |
| 多供应商 LLM | 无 | 完整 | 新增功能 |
| Token 成本统计 | 完整 | 框架有，但 WritingService 未调用 | 管道断开 |

---

## 六、优先修复建议

### 阶段一：打通核心管道（P0，预计 4-6 小时）

1. **接通 WritingService 的 4 个 Phase 方法**：从 V4 的 `cli.py` 迁移创作主循环，每个 Phase 调用对应 Agent，通过 emitter 推送 SSE 事件
2. **接通 CLI 创作主循环**：在 `cli.py` 的 `main()` 中补全 V4 风格的完整创作流程
3. **修复 PreviewEdit 的改写回写**：在 `doRewrite()` 中用 DOM 操作替换选中文本
4. **修复 B-02 导入路径问题**：`config_api.py` 中的 `import backend.core.llm_client` 改为相对导入
5. **修复 B-04 暂停参数不匹配**：统一前后端参数格式

### 阶段二：完善交互闭环（P1，预计 3-4 小时）

6. **实现 confirmAction()**：后端增加 confirm 事件处理接口，前端确认后发送结果
7. **SSE 按 work_id 隔离**：emitter 支持按作品过滤事件
8. **统一前端 HTTP 拦截器**：创建 `utils/request.js`，统一错误处理
9. **增加删除确认弹窗**：WorksList 删除作品前二次确认
10. **接入成本追踪到 WritingService**：让创作报告页能显示实际成本数据

### 阶段三：质量提升（P2，预计 2-3 小时）

11. **SSE 按作品隔离**：避免多作品创作时事件混乱
12. **作品数据增量更新**：避免每次全量重写 JSON
13. **前端加载骨架屏**
14. **修复生产环境静态文件托管**
15. **日志自动清理**

---

## 七、总结

V5 的架构方向是正确的，从 CLI 到 GUI 的升级路径设计合理。前端的页面框架、UI 交互、视觉设计都已达到可用水平。后端的 API 层和核心 Agent 层代码质量高，特别是 `llm_client.py` 的鲁棒性和多供应商架构值得肯定。

**核心问题只有一个：WritingService 没有真正调用 Agent。** 这是整个 V5 的「最后一公里」——前面的路都修好了，就差把桥接通。一旦 WritingService 的 4 个 Phase 方法接入了 V4 的 Agent 调用逻辑，V5 就能从「空壳演示」变成「真正可用的产品」。

建议优先集中精力完成阶段一（核心管道打通），这大约需要 4-6 小时的开发时间，但能让 V5 从演示级跃升到可用级。
