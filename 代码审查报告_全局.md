# 奎木狼AI小说创作系统 V6 - 全局代码审查报告

**审查时间**: 2026-04-10
**审查人**: code-reviewer
**项目路径**: c:/Users/31072/WorkBuddy/20260409140801/奎木狼AI小说创作系统_V6

---

## 一、总体评价

项目整体架构清晰，采用了模块化设计。但存在若干安全隐患、并发处理问题和代码规范不一致的问题需要关注。

**整体质量评估**: B+ (良好，有改进空间)

---

## 二、安全隐患检测

### 2.1 严重问题 (必须修复)

#### 问题1: CORS配置不安全 ⚠️ 严重

**位置**: `main.py` 第35-41行

**问题描述**:
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],      # 允许所有来源
    allow_credentials=True,    # 允许携带凭证
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**风险**: `allow_origins=["*"]` 与 `allow_credentials=True` 同时使用会削弱 CORS 安全性，允许任意恶意网站获取用户凭证。

**修复建议**:
```python
# 开发环境可使用：
allow_origins=["http://localhost:5173", "http://127.0.0.1:5173"] if DEBUG else []

# 生产环境应指定具体域名
allow_origins=["https://your-domain.com"]
```

---

#### 问题2: 路径遍历风险 ⚠️ 严重

**位置**: `api/works.py` 第50-51行

**问题描述**:
```python
def get_work_file(work_id: str) -> Path:
    return WORKS_DIR / f"{work_id}.json"
```

**风险**: 虽然当前 work_id 由 `uuid.uuid4().hex[:12]` 生成（随机12位字符串），但如果未来改为用户可控的字符串，可能存在路径遍历风险（如 `../../etc/passwd`）。

**修复建议**:
```python
def get_work_file(work_id: str) -> Path:
    # 严格验证work_id格式：仅允许字母、数字、连字符
    import re
    if not re.match(r'^[a-zA-Z0-9\-]+$', work_id):
        raise ValueError("Invalid work_id format")
    return WORKS_DIR / f"{work_id}.json"
```

---

#### 问题3: API Key内存中明文存储 ⚠️ 中等

**位置**: `core/llm_client.py` 第199-210行, `core/config.py`

**问题描述**:
- API Key 在加载后以明文形式存储在 `_client_cache` 中
- 配置对象 `LLMConfig` 包含明文 API Key

**风险**: 如果服务器被攻破或内存被Dump，API Key可能泄露。

**修复建议**:
1. 不在前端暴露 API Key
2. 考虑使用环境变量而非配置文件存储敏感信息
3. 如果使用 JSON 存储，避免将实际 Key 写入磁盘

---

### 2.2 低风险问题

#### 问题4: 无SQL注入风险 ✅

**说明**: 系统使用 JSON 文件存储（`WORKS_DIR`），没有使用 SQLite 或其他数据库，因此不存在 SQL 注入风险。

---

## 三、代码质量问题

### 3.1 llm_client.py 第189-224行分析 ✅ 无重复定义

**审查结论**: 经确认，第189-224行是 `_repair_json()` 函数的多级JSON修复策略实现，不是重复定义。该函数包含4级修复：
1. 直接解析
2. 提取后解析
3. 单引号转双引号
4. 截断到最后一个完整顶层key-value

---

### 3.2 Prompt动态加载问题 ⚠️ 部分问题

**位置**: `core/prompt_loader.py`

**问题分析**:
```python
_PROMPT_VARS = {
    "TARGET_WORD_COUNT": TARGET_WORD_COUNT,  # 静态导入的值
    "PART_COUNT": PART_COUNT,
    ...
}
```

**问题**:
1. `_PROMPT_VARS` 在模块加载时创建，使用静态导入的常量值
2. 如果运行中 `PART_COUNT` 等配置变更，Prompt 中的占位符不会自动更新
3. 需要调用 `reload_config()` 后才能刷新

**修复建议**:
```python
def load_prompt(prompt_name: str, default: str) -> str:
    # 每次调用时动态获取最新配置
    from core.config import get_app_config
    cfg = get_app_config()
    prompt_vars = {
        "TARGET_WORD_COUNT": cfg.target_word_count,
        "PART_COUNT": cfg.part_count,
        "PART_WORD_MIN": cfg.part_word_min,
        "PART_WORD_MAX": cfg.part_word_max,
    }
    ...
```

---

### 3.3 全局状态管理问题 ⚠️ 需关注

**位置**: `services/writing_service.py` 第87-91行

**问题描述**:
```python
# 全局写作状态（跨请求共享）
_writing_state: dict = {}
_pause_flags: dict = {}
_confirm_flags: dict = {}
```

**风险**:
1. 全局字典在多线程/异步环境下缺乏锁保护
2. 多作品同时创作时，可能产生状态竞争
3. `_writing_state` 是普通字典，非线程安全

**修复建议**:
```python
import threading
_state_lock = threading.Lock()

# 或使用 asyncio 的锁
_pause_flags: dict = {}
_confirm_flags: dict = {}

# 在访问 _writing_state 时使用锁
with _state_lock:
    _writing_state[work_id] = {...}
```

---

### 3.4 并发写入冲突风险 ⚠️ 中等

**位置**: `core/session_manager.py`, `api/works.py`

**问题描述**:
多个并发请求同时修改同一作品文件时，没有文件锁机制：

```python
# session_manager.py 第84-89行
def save_session(self, session_id: str, story_state: StoryState) -> bool:
    story_state.save(self._get_state_path(session_id))  # 直接写入
    self._update_session_timestamp(session_id)  # 再写入一次
```

**修复建议**:
```python
import fcntl
import os

def atomic_write(filepath: Path, content: str):
    """原子写入：先写临时文件，再重命名"""
    temp = filepath.with_suffix('.tmp')
    temp.write_text(content, encoding='utf-8')
    os.replace(temp, filepath)
```

---

## 四、性能问题

### 4.1 长上下文处理 ⚠️ 需关注

**位置**: `core/story_state.py` 第88-169行

**问题描述**:
`get_part_context()` 方法随着 Part 数量增加，上下文会不断累积：
- 每个 Part 需要加载全部历史摘要
- 上一 Part 结尾取最后1200字
- 角色状态快照会包含所有已完成 Part 的信息

**风险**: 对于20+ Part 的长篇作品，上下文可能达到数十KB，影响 LLM 调用速度和成本。

**修复建议**:
```python
def get_part_context(self, part_num: int, max_history_parts: int = 5) -> str:
    """限制历史上下文长度，只保留最近N个Part"""
    completed_parts = sorted(self.part_summaries.keys())
    # 只保留最近5个Part的详细摘要
    recent_parts = completed_parts[-max_history_parts:] if len(completed_parts) > max_history_parts else completed_parts
    ...
```

---

### 4.2 内存泄漏风险 ⚠️ 需关注

**位置**: `core/llm_client.py` 第193-210行

**问题描述**:
```python
_client_cache: dict = {}  # 永不清理的缓存
```

**风险**:
- 每个 Agent 的客户端被缓存后永不释放
- 如果动态切换 Agent 配置，旧的客户端仍占用内存

**修复建议**:
```python
import threading
_client_lock = threading.Lock()
MAX_CACHE_SIZE = 10

def _get_client_for_agent(agent: str) -> tuple:
    with _client_lock:
        if agent in _client_cache:
            return _client_cache[agent]
        if len(_client_cache) >= MAX_CACHE_SIZE:
            # LRU淘汰策略
            oldest = next(iter(_client_cache))
            del _client_cache[oldest]
        ...
```

---

## 五、代码规范问题

### 5.1 日志使用不统一 ⚠️ 需改进

**位置**: 多处

**问题描述**:
部分代码使用 `print()` 而非统一的日志系统：
```python
# works.py 第69行
except:
    continue  # 静默忽略错误，无日志

# session_manager.py 第91行
print(f"保存会话失败: {e}")
```

**修复建议**:
```python
import logging
logger = logging.getLogger(__name__)

# 使用标准日志
logger.error(f"保存会话失败: {e}")
```

---

### 5.2 错误处理不一致 ⚠️ 需改进

**位置**: `api/works.py` 第69行

**问题描述**:
```python
try:
    data = json.loads(f.read_text(encoding="utf-8"))
    works.append({...})
except:
    continue  # bare except，过于宽泛
```

**修复建议**:
```python
try:
    data = json.loads(f.read_text(encoding="utf-8"))
    works.append({...})
except json.JSONDecodeError as e:
    logger.warning(f"作品文件 {f} 格式错误: {e}")
    continue
except Exception as e:
    logger.error(f"读取作品文件 {f} 失败: {e}")
    continue
```

---

### 5.3 代码注释不完整 ⚠️ 建议改进

**位置**: 多处

**问题描述**:
部分关键函数缺少参数和返回值说明：
```python
# works.py 第50-51行
def get_work_file(work_id: str) -> Path:
    return WORKS_DIR / f"{work_id}.json"
    # 缺少文档注释
```

**修复建议**: 按 Google Style 添加完整文档字符串。

---

## 六、问题汇总 (按严重程度)

### 严重 (必须修复)
1. **CORS配置不安全** - `allow_origins=["*"]` + `allow_credentials=True`
2. **路径遍历风险** - `get_work_file()` 未验证 work_id 格式

### 中等 (建议修复)
3. **并发写入冲突** - JSON文件读写无原子性保证
4. **全局状态非线程安全** - `_writing_state` 字典无锁保护
5. **API Key明文存储** - 内存中以明文保存敏感信息

### 轻微 (建议改进)
6. **Prompt变量静态绑定** - 配置变更后不自动刷新
7. **内存泄漏风险** - `_client_cache` 永不清理
8. **日志不统一** - 混用 print 和 logger
9. **错误处理不规范** - bare except 使用
10. **长上下文风险** - get_part_context 无限制增长

---

## 七、修复优先级建议

### P0 - 立即修复
1. 修复 CORS 配置，指定具体允许的来源
2. 为 `get_work_file()` 添加 work_id 格式验证

### P1 - 短期内修复
3. 为全局状态操作添加锁保护
4. 实现文件写入原子性（临时文件+重命名）
5. 统一日志系统，废弃 print 语句

### P2 - 后续优化
6. 改进 Prompt 变量动态加载
7. 实现 LLM 客户端缓存 LRU 淘汰
8. 限制 get_part_context 历史长度
9. 完善代码注释和文档

---

## 八、代码亮点

项目中也有值得肯定的实现：

1. **错误处理系统** (`error_handler.py`) - 定义了清晰的错误类型层次结构
2. **JSON多级修复** (`llm_client.py`) - 健壮的 JSON 解析和修复策略
3. **SSE事件系统** (`sse.py`) - 支持按 work_id 隔离的事件推送
4. **StoryLogger** (`logger.py`) - 完整的创作过程日志记录
5. **Pydantic模型验证** - API 请求使用 Pydantic 进行基本验证

---

*报告生成时间: 2026-04-10 20:46*