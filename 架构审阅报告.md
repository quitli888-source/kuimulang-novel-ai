# 奎木狼AI小说创作系统 V6 架构审阅报告

> 审阅日期：2026-04-10
> 审阅人：AI架构师Agent

---

## 一、现有架构总览

### 1.1 技术栈

| 层级 | 技术 | 评价 |
|------|------|------|
| 后端框架 | FastAPI + Uvicorn | 现代异步框架，选型合理 |
| 前端框架 | Vue 3 + Vite + Naive UI | 主流技术栈，开发效率高 |
| 状态管理 | Pinia | 轻量级，适合中小型应用 |
| 实时通信 | SSE | 轻量级实时推送，方案成熟 |
| LLM集成 | OpenAI兼容接口 | 适配多供应商，灵活性好 |
| 打包部署 | PyInstaller | 桌面应用打包成熟方案 |

### 1.2 核心模块关系

```
用户请求
    │
    ▼
┌─────────────────────────────────────────────────────┐
│                    FastAPI Backend                    │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│  │ Works   │  │ Writing │  │ Rewrite │  │  SSE    │ │
│  │ API     │  │ Service │  │ Service │  │Emitter  │ │
│  └────┬────┘  └────┬────┘  └─────────┘  └────┬────┘ │
│       │            │                          │      │
│       ▼            ▼                          │      │
│  ┌─────────────────────────────────────┐      │      │
│  │           WritingService             │◄─────┘      │
│  │  Phase1→Phase2→Phase3→Phase4       │             │
│  └──────────────────┬──────────────────┘             │
│                     │                                 │
│       ┌─────────────┼─────────────┐                  │
│       ▼             ▼             ▼                  │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐             │
│  │Planning  │  │Writing  │  │Optimizer│             │
│  │ Agents   │  │ Agents  │  │ Agents  │             │
│  └────┬─────┘  └────┬────┘  └────┬────┘             │
│       │             │             │                  │
│       └─────────────┼─────────────┘                  │
│                     ▼                                 │
│              ┌──────────────┐                        │
│              │ StoryState   │                        │
│              │ (共享状态)   │                        │
│              └──────────────┘                        │
└─────────────────────────────────────────────────────┘
                     │
                     ▼ (SSE实时推送)
┌─────────────────────────────────────────────────────┐
│                   Vue 3 Frontend                      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐            │
│  │ Works   │  │ Writing  │  │ Preview │            │
│  │ List    │  │Progress  │  │  Edit   │            │
│  └─────────┘  └─────────┘  └─────────┘            │
└─────────────────────────────────────────────────────┘
```

---

## 二、架构优势分析

### 2.1 优势

1. **多Agent协作系统设计合理**
   - 各Agent职责单一：PlanningAgent、PlotPlannerAgent、PartWriterAgent、StyleOptimizerAgent、LogicReviewAgent、EmotionReviewAgent、ConsistencyReviewAgent
   - 通过StoryState共享数据，耦合度低
   - 支持每个Agent独立配置LLM供应商

2. **创作流程阶段化**
   - Phase1灵感解析 → Phase2大纲规划 → Phase3逐Part创作 → Phase4优化审核
   - 支持暂停/恢复、手动确认模式
   - SSE实时推送各阶段进度

3. **LLM客户端封装完善**
   - 支持思维链标签过滤（MiniMax/deepseek-r1）
   - 多级JSON修复策略，兼容性强
   - Token成本追踪

4. **前端工程化程度高**
   - Vue 3 + Composition API
   - Pinia状态管理
   - 4种主题切换系统
   - 组件化设计，结构清晰

---

## 三、架构问题与改进建议

### 3.1 数据层问题 (P0)

**问题**：纯JSON文件存储，存在以下风险：
- 并发写入时可能数据丢失
- 大量作品时检索性能差
- 无法支持复杂查询（如按题材、时间筛选）

**改进建议**：
```python
# 方案A：引入SQLite（轻量级，推荐）
# 方案B：引入TinyDB（嵌入式，零配置）
# 方案C：保持JSON但增加文件锁和增量更新
```

**实施优先级**：P0，建议使用SQLite替代JSON文件存储

---

### 3.2 状态管理问题 (P0)

**问题**：
- `WritingService`使用全局字典`_writing_state`管理状态
- 多用户/多作品并发时可能冲突
- 状态生命周期管理不清晰

**改进建议**：
```python
# 使用类封装状态，明确生命周期
class WritingSession:
    def __init__(self, work_id: str):
        self.work_id = work_id
        self.state = WritingState(work_id)
        self.start_time = datetime.now()

    async def __aenter__(self):
        return self

    async def __aexit__(self, exc_type, exc_val, exc_tb):
        # 清理资源
        pass
```

**实施优先级**：P0

---

### 3.3 错误处理不统一 (P1)

**问题**：
- `llm_client.py`定义了`NetworkError`、`SystemError`、`LLMError`
- 但实际使用时部分地方直接`raise Exception`
- 错误码不统一，前端难以针对性处理

**改进建议**：
```python
# 统一错误码定义
class ErrorCode:
    NETWORK_ERROR = "E1001"
    LLM_ERROR = "E1002"
    VALIDATION_ERROR = "E2001"
    NOT_FOUND = "E3001"

# 统一错误响应格式
class APIError(Exception):
    def __init__(self, code: str, message: str, details: dict = None):
        self.code = code
        self.message = message
        self.details = details or {}
```

**实施优先级**：P1

---

### 3.4 长上下文挑战 (P1)

**问题**：
- `get_part_context()`在20个Part时上下文超长
- LLM调用可能超过max_tokens限制
- 成本和延迟都会增加

**改进建议**：
```python
# 方案A：关键情节摘要 + 滑动窗口
# 保留最近N个Part的完整结尾 + 早期Part的关键事件

# 方案B：角色状态向量化
# 用向量数据库存储角色状态，检索相关角色历史

# 方案C：动态上下文压缩
# 基于当前Part涉及的角色/事件，动态构建上下文
```

**实施优先级**：P1

---

### 3.5 前端架构问题 (P2)

**问题**：
- `api/index.js`是新建的统一拦截器，但有些页面仍在直接使用axios
- 缺少请求重试机制
- 缺少请求取消（AbortController）支持

**改进建议**：
```javascript
// 增强request工具
const api = axios.create({...})

// 添加重试拦截器
api.interceptors.response.use(
  response => response,
  async error => {
    if (error.config && !error.config.__retryCount) {
      error.config.__retryCount = 0
    }
    if (error.config.__retryCount < 3) {
      error.config.__retryCount++
      await delay(1000 * error.config.__retryCount)
      return api(error.config)
    }
    throw error
  }
)
```

**实施优先级**：P2

---

### 3.6 安全性问题 (P1)

**问题**：
- CORS允许所有来源(`allow_origins=["*"]`)
- API Key通过配置文件明文存储
- 无认证/鉴权机制

**改进建议**：
```python
# 短期：限制CORS范围到已知域名
# 中期：添加简单token认证
# 长期：引入JWT或OAuth2

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173", "http://127.0.0.1:5173"],
    ...
)
```

**实施优先级**：P1（安全性必须重视）

---

### 3.7 日志系统不统一 (P2)

**问题**：
- 部分代码使用`print()`，部分使用`logging`
- GUI模式下print输出不可见
- 日志无分级、无轮转

**改进建议**：
```python
# 统一使用Python logging
import logging
logger = logging.getLogger("kuaimulang")

# 在main.py中配置
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
    handlers=[
        logging.FileHandler("logs/app.log"),
        logging.StreamHandler()
    ]
)
```

**实施优先级**：P2

---

## 四、优化优先级汇总

| 优先级 | 问题 | 工作量 | 风险 |
|--------|------|--------|------|
| P0 | 数据层：JSON→SQLite | 中 | 低 |
| P0 | 状态管理：会话封装 | 小 | 低 |
| P1 | 错误处理统一化 | 小 | 低 |
| P1 | 长上下文优化 | 大 | 中 |
| P1 | 安全性加固 | 中 | 低 |
| P2 | 前端请求拦截增强 | 小 | 低 |
| P2 | 日志系统统一 | 小 | 低 |

---

## 五、技术债务

1. **llm_client.py第189-224行**：函数定义重复，需清理
2. **Prompt动态加载**：模块加载时缓存，不支持运行时更新
3. **版本号分散**：多处定义，应统一管理
4. **未使用pyc文件**：backend目录有大量.pyc文件，应加入.gitignore

---

## 六、总结

奎木狼AI小说创作系统V6的整体架构设计合理，采用了现代化的前后端分离方案。核心的多Agent协作系统和创作流程设计具有良好的扩展性。主要需要改进的方向是：

1. **数据层的可靠性**（P0）
2. **状态管理的规范性**（P0）
3. **安全性和错误处理**（P1）
4. **长上下文优化**（P1）

建议按照P0→P1→P2的优先级分阶段进行优化。
